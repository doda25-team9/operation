apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  labels:
    grafana_dashboard: "true"
data:
  app-metrics.json: |
{{ .Files.Get "dashboards/app-metrics.json" | indent 4 }}

  ab-testing.json: |
{{ .Files.Get "dashboards/ab-testing.json" | indent 4 }}
