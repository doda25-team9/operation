{{- if .Values.shadowLaunch.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.modelService.name }}-deployment-v3
  labels:
    app: {{ .Values.modelService.labels.app }}
    version: v3  
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
spec:
  replicas: {{ .Values.shadowLaunch.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.modelService.labels.app }}
      version: v3
  template:
    metadata:
      labels:
        app: {{ .Values.modelService.labels.app }}
        version: v3
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.modelService.service.port }}"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: model
        image: "{{ .Values.modelService.image.repository }}:{{ .Values.shadowLaunch.modelImageTag }}"
        imagePullPolicy: {{ .Values.modelService.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.modelService.service.port }}
          name: http
        
        env:
        - name: MODEL_VERSION
          value: "{{ .Values.shadowLaunch.modelVersion }}"
        - name: SMTP_USER 
          valueFrom: 
            secretKeyRef: 
              name: {{ .Values.secret.name }} 
              key: {{ .Values.secret.smtpUserKey }} 
        - name: SMTP_PASS 
          valueFrom: 
            secretKeyRef: 
              name: {{ .Values.secret.name }} 
              key: {{ .Values.secret.smtpPassKey }}
        volumeMounts:
        - name: model-storage
          mountPath: /mnt/shared
        
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
      
      volumes:
      - name: model-storage
        hostPath:
          path: {{ .Values.modelService.volume.hostPath }}
          type: DirectoryOrCreate
{{- end }}